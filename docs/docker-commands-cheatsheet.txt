# Docker & Docker Compose Common Commands Cheat Sheet

## Docker Compose (multi-container)

# Start all services (foreground)
docker-compose up

# Start all services and rebuild images
docker-compose up --build

# Start all services in the background (detached)
docker-compose up -d

# Stop all services
docker-compose down

# View logs for all services
docker-compose logs

# View logs for a specific service
docker-compose logs <service_name>

# Rebuild a specific service
docker-compose up --build <service_name>


## Docker (single container/image)

# List running containers
docker ps

# List all containers (including stopped)
docker ps -a

# List all images
docker images

# Stop a running container
docker stop <container_id>

# Remove a container
docker rm <container_id>

# Remove an image
docker rmi <image_id>

# View logs for a container
docker logs <container_id>

# Open a shell inside a running container
docker exec -it <container_id> /bin/bash 

aham cmnd mn 3and zaghloul: docker system prune -a




 npm run lint     
 npx eslint src/app/components/Header.js   
 NPX: for direct execution
 NPM: for installed packages/scripts
 recharts: Data Visualization
 lucide-react: for icons
 react-dom: 
    - Page loads - Renders initial HTML
    - User clicks buttons - Handles click events
    - Forms are filled - Manages input values
    - State changes - Updates DOM efficiently
    - Navigation occurs - Renders new pages
    - Components mount/unmount - Manages lifecycle